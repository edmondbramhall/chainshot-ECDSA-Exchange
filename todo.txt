Validate amount input client-side for consistency
Show total amount in the exchange in the UI, and validate that we always have
  the same amount in the server
Can we move code into a shared class, does that work?
Try writing some tests for the server-side code.
